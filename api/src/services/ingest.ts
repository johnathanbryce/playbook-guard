// Full ingest pipeline: hash-dedup -> store contract -> chunk -> embed -> store chunks.
export async function ingest(filename: string, rawText: string): Promise<unknown> {
  throw new Error("TODO(John)");
}
